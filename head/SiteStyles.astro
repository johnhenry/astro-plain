---
type Props = {
  hrefs?: string[],
  critical?: string,
}
const {hrefs=[], critical=""}:Props = Astro.props;
---
{critical ?
`<style>
${critical}
</style>` : null}
{hrefs.map(href=>
  <link rel="stylesheet" href={href} />)
}